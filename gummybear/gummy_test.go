package gummybear_test

import (
	. "github.com/concourse/atc/gummybear"

	. "github.com/onsi/ginkgo"
	. "github.com/onsi/gomega"
)

var _ = Describe("CheckForVersions", func() {
	It("creates a check container based on the config", func() {})
	It("runs the check script", func() {})
	It("returns the versions from the check", func() {})

	Context("when the type required has no versions", func() {
	})
})
